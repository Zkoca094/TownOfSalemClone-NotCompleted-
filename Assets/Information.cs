using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Information : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        /*
        VOTING HAS BEGUN
        DISCUSSION TIME
        PLAYERNAME HAS BEEN A HEADMAN
         */
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
